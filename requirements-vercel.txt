# Vercel-Optimized Requirements
# Minimal dependencies for Vercel serverless deployment
# Core functionality only - optional features removed

# Core Framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Database
# MySQL support (for PlanetScale, Railway, etc.)
pymysql>=1.1.0
# PostgreSQL support (for Vercel Postgres, Supabase, etc.)
psycopg2-binary>=2.9.9
# SQLAlchemy (works with both MySQL and PostgreSQL)
sqlalchemy>=2.0.23
alembic>=1.12.1

# Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# AI & ML (Core - Required)
openai>=1.3.5
anthropic>=0.18.1  # Claude API
google-generativeai>=0.3.2  # Gemini API

# Google Cloud Services (Optional - only if using)
# google-cloud-vision>=3.4.5
# google-cloud-documentai>=2.20.1

# OCR Services (Optional - only if using)
# azure-cognitiveservices-vision-computervision>=0.9.0
# msrest>=0.7.1

# Basic Image Processing
pillow>=10.1.0
pytesseract>=0.3.10  # Basic OCR fallback

# PDF Processing
PyPDF2>=3.0.1
pdfplumber>=0.10.3

# Image Processing (Minimal)
numpy>=1.26.0  # Python 3.12 compatible

# File Storage (Optional - only if using)
boto3>=1.29.7
# cloudinary>=1.38.0  # Comment out if not using Cloudinary

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Utils
python-dateutil>=2.8.2
pytz>=2023.3

# Note: Removed heavy dependencies:
# - opencv-python (use opencv-python-headless if needed)
# - spacy, nltk (NLP - optional)
# - transformers, torch (TrOCR - optional)
# - pdf2image (optional)
