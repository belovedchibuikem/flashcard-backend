# Core Framework (Minimal for Vercel)
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Database
# MySQL support (for PlanetScale, Railway, etc.)
pymysql>=1.1.0
# PostgreSQL support (for Vercel Postgres, Supabase, Neon, etc.)
psycopg2-binary>=2.9.9
# SQLAlchemy (works with both MySQL and PostgreSQL)
sqlalchemy>=2.0.23
alembic>=1.12.1

# Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0  # Required for EmailStr type in Pydantic

# AI & ML (Core - Required)
openai>=1.3.5
anthropic>=0.18.1  # Claude API
google-generativeai>=0.3.2  # Gemini API

# Basic Image Processing (Lightweight - Pillow only)
pillow>=10.1.0
# pytesseract REMOVED - requires system libraries, won't work on Vercel
# Use external OCR APIs instead (Google Vision, Azure, etc.)

# PDF Processing (Lightweight)
PyPDF2>=3.0.1
# pdfplumber REMOVED - too large for Vercel (~50MB+)
# Use PyPDF2 for basic PDF text extraction, or external APIs for advanced processing

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Utils
python-dateutil>=2.8.2
pytz>=2023.3
httpx>=0.25.0

# REMOVED HEAVY DEPENDENCIES (to stay under 250MB limit):
# - gunicorn (not needed on Vercel - uses uvicorn)
# - pytesseract (requires system libraries - use external OCR APIs)
# - pdfplumber (~50MB+ - use PyPDF2 or external APIs)
# - numpy (pillow may work without it, but add back if needed)
# - google-cloud-vision (~100MB+ - use REST API instead)
# - google-cloud-documentai (~100MB+ - use REST API instead)
# - azure-cognitiveservices-vision-computervision (~30MB+ - use REST API instead)
# - opencv-python-headless (~50MB+ - use external APIs)
# - boto3 (~30MB+ - use REST API instead)
# - cloudinary (only if not using Cloudinary)
# - spacy, nltk (very large NLP libraries)
# - transformers, torch (~500MB+ - use external APIs)


